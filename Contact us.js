function handleSubmit(event) {
    event.preventDefault(); // Prevents the default form submission behavior

    // Display a prompt thanking the user
    command.prompt('Thank you! Your query has been submitted.');}